<!DOCTYPE html>
<html lang="en">
    <head>
        {% include '../_includes/partials/head.njk' %}
        {# {% block head %}{% endblock %} #}
    </head>
    <body>
        <nav>
            <div class="wrapper">
                <ul>
                    {% for item in nav %}
                        <li {% if page.url == item.url %} aria-current="page" {% endif %}>
                            <a href="{{ item.url }}">{{ item.name }}</a>
                        </li>
                    {% endfor %}
                </ul>
            </div>
        </nav>
        <header>
            <div class="wrapper">
                <h1>{{ title }}</h1>
                <mark>{{ page.url }}</mark>
            </div>
        </header>
        <main>
            <div class="wrapper">
                {{ content | safe }}
            </div>
        </main>
        <footer>
            <div class="wrapper">
                <p>&#169; 2023.</p>
            </div>
        </footer>
    </body>
</html>