---
title: Archive
---

<ul class="tag-catg">
  {% for tag, posts in collections | dictsort %}
  {% if tag !== "all" and tag !== "post" %}
    <li><a href="/tags/{{tag}}/">{{tag}} <span aria-hidden="true">{{posts.length}}</span></a></li>
  {% endif %}
  {% endfor %}
</ul>

<br>
<hr>
<br>

<ul>

  {% for post in collections.post %}
      <li> <a href="{{post.url}}">{{ post.data.title }}</a></li>
  {% endfor %}
</ul>